const withImages = require('next-images');
module.exports = {
    future: {
        webpack5: true,
    },

    // webpack(config) {
    //     config.resolve.alias = {
    //         ...config.resolve.alias,
    //         // your aliases
    //     }
    // }
}
